extends layout
block content
 h1= title
 if (toShow == null)
  h2 No instance to update
 else
  - let vehicleid = toShow._id
  - let vehicle_type = toShow.vehicle_type
  - let vehiclecost = toShow.cost
  - let vehicleize = toShow.size
  div.row(class='mb-2')
   div.mr-1 ID is
   div #{vehicleid}
  -let actionid = "/resource/vehicle/" + vehicleid
  form(id='vehicleForm', action= actionid, method="PUT")
   div.form-group
     label.vehicleAttr(for='company') vehicle company
     input#company(type='String', name='company', value= company )
   div.form-group
     label.vehicleAttr(for='price') vehicle price
     input#price(type='number', name='price', value= price ,min=3,max=100)
   div.form-group
     label.vehicleAttr(for='color') vehicle color
     input#color(type='String', name='color', value= color )
   input(type="submit")
  script.
   document.forms['vehicleForm'].addEventListener('submit', (event) => {
    event.preventDefault();
    // TODO do something here to show user that form is being submitted
    fetch(event.target.action, {
     method: 'PUT',
     body: new URLSearchParams(new FormData(event.target)) // event.target is the form
    }).then((resp) => {
    return resp.json(); // or resp.text() or whatever the server sends
    }).then((body) => {
     alert("Update succeded")
     // TODO handle body
    }).catch((error) => {
     // TODO handle error
     alert("Update failed " + error )
    });
   });